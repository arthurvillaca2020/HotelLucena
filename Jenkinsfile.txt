pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // Clona o repositório do GitHub
                git 'https://github.com/tassioferenzini/Hotel_Lucena.git'
            }
        }
        stage('Build') {
            steps {
                // Executa o comando Maven para compilar o projeto
                sh 'mvn clean package'
            }
        }
         stage('Test') {
            steps {
                // Executa os testes unitários
                sh 'mvn test'
            }
        }
    }
}
