pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // Clona o repositório do GitHub
                git 'https://github.com/arthurvillaca2020/HotelLucena.git'
            }
        }
        stage('Build') {
            agent {
                // Clona o repositório do GitHub
                any { image 'maven' }
            }
        }
    }
}
